export enum Token {
  FORGETPASSWORD = "forget_password_token",
  ACCESS = "access_token",
  REFRESH = "refresh_token",
}

export enum WebMartUserType {
  ADMIN = "admin",
  USER = "user",
  SELLER = "seller"
}

export enum ApiMethods {
  GET = "GET",
  PUT = "PUT",
  POST = "POST",
  PATCH = "PATCH",
  DELETE = "DELETE",
}

export enum Actions {
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
  LIST = 'list',
}